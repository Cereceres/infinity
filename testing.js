'use strict'
let id = module.exports = function (length) {
    let n = 20+Math.floor(Math.random()*15)
    let step = 2+Math.floor(Math.random()*3)
    let gen = function () {
        Date.now().toString()
    }
}
